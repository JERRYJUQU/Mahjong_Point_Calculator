package src.main

class Calculator {
    fun calculate (hand : Hand) : Void {
        if (hasBigFourWinds(hand.melds)) {
            hand.points +=
        }
        if (bigThreeDragons(hand.melds))
    }
}